public
node_modules
.quartz-cache

# Generated by blog:sync and required to match the vault byte for byte —
# `pnpm format` reformatting it would desync it and the next sync would
# silently overwrite the result.
content

# Hand-laid-out SVG on a 4px grid. Prettier's wrapping makes the coordinates
# unreadable, and the .html wrappers must stay comparable to the .svg.frag
# sources they are built from.
diagrams
